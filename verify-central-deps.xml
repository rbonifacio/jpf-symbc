<?xml version="1.0" encoding="UTF-8"?>
<!--
  Temporary POM for Task 1.4: Verify 8 Maven Central coordinates resolve.
  Run: mvn -f verify-central-deps.xml dependency:resolve
  Then delete this file.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>gov.nasa.jpf</groupId>
  <artifactId>verify-central-deps</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Verify Maven Central Dependencies</name>
  <description>Temporary POM to verify that the 8 Maven Central JARs from lib/ have valid coordinates</description>

  <dependencies>
    <!-- 1. commons-lang:commons-lang:2.4 (legacy groupId) -->
    <dependency>
      <groupId>commons-lang</groupId>
      <artifactId>commons-lang</artifactId>
      <version>2.4</version>
    </dependency>

    <!-- 2. commons-math:commons-math:1.2 (legacy groupId) -->
    <dependency>
      <groupId>commons-math</groupId>
      <artifactId>commons-math</artifactId>
      <version>1.2</version>
    </dependency>

    <!-- 3. org.apache.bcel:bcel:6.0 -->
    <dependency>
      <groupId>org.apache.bcel</groupId>
      <artifactId>bcel</artifactId>
      <version>6.0</version>
    </dependency>

    <!-- 4. dk.brics.automaton:automaton:1.11-8 -->
    <dependency>
      <groupId>dk.brics.automaton</groupId>
      <artifactId>automaton</artifactId>
      <version>1.11-8</version>
    </dependency>

    <!-- 5. jaxen:jaxen:1.2.0 -->
    <dependency>
      <groupId>jaxen</groupId>
      <artifactId>jaxen</artifactId>
      <version>1.2.0</version>
    </dependency>

    <!-- 6. com.martiansoftware:jsap:2.1 -->
    <dependency>
      <groupId>com.martiansoftware</groupId>
      <artifactId>jsap</artifactId>
      <version>2.1</version>
    </dependency>

    <!-- 7. com.googlecode.aima-java:aima-core:0.10.5 -->
    <dependency>
      <groupId>com.googlecode.aima-java</groupId>
      <artifactId>aima-core</artifactId>
      <version>0.10.5</version>
    </dependency>

    <!-- 8. redis.clients:jedis:2.0.0 -->
    <dependency>
      <groupId>redis.clients</groupId>
      <artifactId>jedis</artifactId>
      <version>2.0.0</version>
    </dependency>
  </dependencies>
</project>
