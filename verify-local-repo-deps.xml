<?xml version="1.0" encoding="UTF-8"?>
<!--
  Temporary POM for Task 1.3: Verify 20 local repo JARs resolve.
  Run: mvn -f verify-local-repo-deps.xml dependency:resolve
  Then delete this file.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>gov.nasa.jpf</groupId>
  <artifactId>verify-local-repo-deps</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Verify Local Repository Dependencies</name>
  <description>Temporary POM to verify that all 20 local repo JARs are resolvable</description>

  <repositories>
    <repository>
      <id>project-local-repo</id>
      <url>file://${project.basedir}/repo</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
  </repositories>

  <dependencies>
    <!-- 1. coral -->
    <dependency>
      <groupId>com.ufpe</groupId>
      <artifactId>coral</artifactId>
      <version>0.7</version>
    </dependency>

    <!-- 2. green -->
    <dependency>
      <groupId>za.ac.sun</groupId>
      <artifactId>green</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 3. hampi -->
    <dependency>
      <groupId>edu.stanford</groupId>
      <artifactId>hampi</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 4. iasolver -->
    <dependency>
      <groupId>com.iastate</groupId>
      <artifactId>iasolver</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 5. libcvc3 2.3.0 -->
    <dependency>
      <groupId>edu.nyu</groupId>
      <artifactId>libcvc3</artifactId>
      <version>2.3.0</version>
    </dependency>

    <!-- 6. libcvc3 5.0.0 (separate dep entry since different version) -->
    <!-- Note: Maven will only resolve one version per groupId:artifactId in a single POM.
         The 5.0.0 version is verified by its presence in the repo structure. -->

    <!-- 7. z3 -->
    <dependency>
      <groupId>com.microsoft</groupId>
      <artifactId>z3</artifactId>
      <version>4.8.14</version>
    </dependency>

    <!-- 8. choco -->
    <dependency>
      <groupId>choco</groupId>
      <artifactId>choco</artifactId>
      <version>1.2.04</version>
    </dependency>

    <!-- 9. choco-solver -->
    <dependency>
      <groupId>choco</groupId>
      <artifactId>choco-solver</artifactId>
      <version>2.1.1</version>
    </dependency>

    <!-- 10. opt4j -->
    <dependency>
      <groupId>org.opt4j</groupId>
      <artifactId>opt4j</artifactId>
      <version>2.4</version>
    </dependency>

    <!-- 11. grappa -->
    <dependency>
      <groupId>att.research</groupId>
      <artifactId>grappa</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 12. sat4j core -->
    <dependency>
      <groupId>org.sat4j</groupId>
      <artifactId>org.sat4j.core</artifactId>
      <version>CUSTOM-20100705</version>
    </dependency>

    <!-- 13. sat4j pb -->
    <dependency>
      <groupId>org.sat4j</groupId>
      <artifactId>org.sat4j.pb</artifactId>
      <version>CUSTOM-20100705</version>
    </dependency>

    <!-- 14. scale -->
    <dependency>
      <groupId>edu.ucsb</groupId>
      <artifactId>scale</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 15. solver -->
    <dependency>
      <groupId>edu.ucsb</groupId>
      <artifactId>solver</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 16. string -->
    <dependency>
      <groupId>edu.ucsb</groupId>
      <artifactId>string</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 17. proteus -->
    <dependency>
      <groupId>edu.ucsb</groupId>
      <artifactId>proteus</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 18. statemachines -->
    <dependency>
      <groupId>edu.ucsb</groupId>
      <artifactId>statemachines</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 19. stpjni -->
    <dependency>
      <groupId>org.stp</groupId>
      <artifactId>stpjni</artifactId>
      <version>1.0</version>
    </dependency>

    <!-- 20. yicesapijava -->
    <dependency>
      <groupId>edu.sri</groupId>
      <artifactId>yicesapijava</artifactId>
      <version>1.0</version>
    </dependency>
  </dependencies>
</project>
